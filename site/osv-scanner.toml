[[IgnoredVulns]]
id = "GHSA-5p2g-fcmc-qvqq"
reason = "image-size is a build-time-only Docusaurus dependency that sizes local, trusted repo images at doc build time; it never parses attacker-controlled input. No patched release exists yet upstream (fixed version not published as of 2026-09-01)."

[[IgnoredVulns]]
id = "GHSA-w3rx-r6r6-pgpr"
reason = "image-size is a build-time-only Docusaurus dependency that sizes local, trusted repo images at doc build time; it never parses attacker-controlled input. No patched release exists yet upstream (fixed version not published as of 2026-09-01)."
